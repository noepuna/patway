import { BackNavDirective } from './back-nav.directive';

describe('BackNavDirective', () => {
  it('should create an instance', () => {
    const directive = new BackNavDirective();
    expect(directive).toBeTruthy();
  });
});
